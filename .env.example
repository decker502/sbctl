# 复制此文件为 .env 并填入你的配置
# cp .env.example .env

# 订阅链接（必填）
REMOTE_URL=你的订阅链接

# sing-box 可执行文件路径（可选，默认使用 PATH 中的 sing-box）
# SING_BOX_BIN=/usr/local/bin/sing-box

# 默认代理节点名（可选，使用 ./start.sh list 查看可用节点）
PROXY_NAME="auto"

# 默认要代理的进程名列表（用空格分隔，支持正则）
PROCESS_NAMES="Antigravity Dia"

# Clash API 配置 (yacd Web UI)
# CLASH_API_PORT=9090        # API 监听端口，默认 9090
# CLASH_API_SECRET=          # 访问密码，默认为空
